pipeline {
    agent any

    stages {
        stage('Git Clone') {
            steps {
                echo 'Git Clone'
                sh "git clone https://github.com/gowns764/test-repo.git"
            }
        }
    }